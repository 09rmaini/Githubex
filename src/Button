class Button extends Component {
    render() {
        return (
            <div onClick={this.props.onClick} className="Button">
             {this.props.label}
            </div>
        )
    }
}